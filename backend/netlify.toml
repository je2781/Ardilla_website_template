[build]
    functions = "functions"
    publish =  "dist"

[dev]
    publish = "dist"
    port = 8080

[functions]
    directory = "functions"
    included_files = ["./src/users.json"]

[[redirects]]
    to = "/.netlify/functions/api/:splat"
    from = "/*"
    status = 200